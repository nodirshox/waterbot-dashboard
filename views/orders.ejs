<%- include('includes/header') %>

<div class="col-12">
  <div class="card">
    <div class="card-header">
      <h3 class="card-title">Buyurtmalar</h3>
      <div class="card-tools">
        <ul class="pagination pagination-sm float-right">
          <li class="page-item"><a class="btn btn-block btn-primary" href="<%= channel %>" ><i class="fa fa-location-arrow nav_icon"></i> Kanalga qo'shilish</a></li>
        </ul>
      </div>
    </div>
    <div class="card-body p-0">
      <table class="table table-hover table-bordered">
        <thead>
          <tr>
            <th style="width: 10px">#</th>
            <th>Kim</th>
            <th style="width: 40px">Vaqt</th>
            <th>Holati</th>
          </tr>
        </thead>
        <tbody>
          <% order.map((users, i) => { %>
          <tr onclick="location.href='/order/<%= users._id %>';" class="mycursor">
            <td><%= i+1 %>.</td>
            <td><a href="/order/<%= users._id %>"><%= users.firstname %></a></td>
            <td><%= users.registration.toLocaleString() %></td>
            <td><%if (users.status === 1) { %> 🕔 <% } else if(users.status === 2) { %> ✅ <% } else if(users.status === 0) { %> ❌ <% } %></td>
          </tr>
          <%  }) %>
        </tbody>
      </table>
    </div>
  </div>
</div>

<%- include('includes/footer') %>