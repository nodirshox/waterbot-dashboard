<%- include('includes/header') %>
<div class="col-md-12">
    <div class="card card-primary">
      <div class="card-header">
        <h3 class="card-title">O'zgartirish</h3>
      </div>
    <form action="/edit/<%= product._id %>" method="POST">
        <div class="card-body">
        <div class="form-group">
            <label>Mahsulot nomi</label>
            <input type="text" name="name" class="form-control" value="<%= product.name %>" autocomplete="off" required>
        </div>
        <div class="form-group">
            <label>Mahsulot haqida</label>
            <input type="text" name="description" class="form-control" value="<%= product.description %>" autocomplete="off">
        </div>
        <div class="form-group">
            <label>Narxi</label>
            <input type="number" name="price" class="form-control" value="<%= product.price %>" autocomplete="off" min="1" required>
        </div>
        <div class="form-group">
            <label>Holati</label><br />
            <select id="status" name="status" class="form-control">
                <% for(var i = 0; i <= 2; i++ ) {
                    var myoption
                    var item=""
                    if(product.status === i) {
                        myoption = "selected"
                    } else { myoption = "" }
                    if(i === 0) {
                        item = "Arxiv" 
                    } else if ( i === 1) {
                        item = "Faol"
                    } else if ( i === 2) {
                        item = "Nofaol"
                    } else { itemname = "Xatolik" } %>
                    <option value="<%= i %>" <%= myoption %> ><%= item %></option>
                <% } %>
              </select>
        </div>
        <div class="form-group">
            <label>Tovar uchun rasm havola</label>
            <input type="text" name="picture" class="form-control" value="<%= product.picture %>" autocomplete="off" required>
        </div>
        <button type="submit" class="btn btn-success">Saqlash</button>
    </div>
    </form>
</div>
</div>


<%- include('includes/footer') %>